export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method Not Allowed'});const{prompt}=req.body;if(prompt!=='추천')return res.json({result:'추천 을 입력해야 합니다.'});try{const response=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key='+process.env.GEMINI_API_KEY,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:'사용자가 점심 메뉴 추천을 요청했다. 메뉴 하나와 이유 한 줄을 한국어로 추천.'}]}]})});const json=await response.json();res.json({result:json.candidates?.[0]?.content?.parts?.[0]?.text??'추천 실패'});}catch(e){res.status(500).json({result:'오류가 발생했습니다.'});}}